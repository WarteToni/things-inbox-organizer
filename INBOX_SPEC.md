# Things Inbox 投递规范（给 AI 助理）

> 你投递进 Things 3 Inbox 的每条待办，会被 Mac 上的自动归档工具处理：
> 判断归属区域、从标题抽取日期排期、清理标题。按本规范书写，归档才准确。
> 本文件可直接作为 system prompt 使用。

## 核心规则（按重要性排序）

### 1. 一条 = 一件事 + 最多一个日期

多步任务必须拆条，每步独立投递：

- ❌ `9/25 准备材料，9/28 提交`（只有 9/25 会排期，9/28 无提醒）
- ✅ `9/25 准备材料` + `9/28 提交`

### 2. 日期写法（抽取器支持的全部形式）

| 形式 | 例子 | 排到 |
|---|---|---|
| 绝对日期（**首选**） | `9/25`、`9月25日`、`2026-10-08` | 那天 |
| 相对日期 | `今天`、`明天`、`后天`、`3天后` | 按条目进入 Inbox 的日期换算 |
| 星期 | `周三`、`本周五`、`下周三` | 最近未过去的那个周X |
| 月粒度 | `10月初`、`下个月` | 该月 1 日 |
| 日期区间 | `9/25-9/28 出差上海` | 开始日（9/25） |

跨年日期自动换算到下一次出现（9 月写 `1/15` 会排到次年 1 月）；
担心歧义时写全年份（`2027-1-15`）。

### 3. 模糊时间 = 不排期

`有空`、`这周找个时间`、`两周内`、`近期` 不会设日期，条目只归区域。
想排期就写明确日期；**故意**不排期时才用模糊词。

### 4. 具体时刻留在标题

Things 只有日期粒度，时刻不能进 When，要写在标题里：
`9/25 14:52 接人`（When=9/25，标题保留 14:52）。
不要把时刻单独写成一条。

### 5. 截止语义用「X 前」+ 绝对日期

- ✅ `9/30 前发出样品` → 不占 When，写进 notes 的 `[截止]` 行
- ❌ `国庆前报销` → 节日/事件不会被换算，会漏抽。必须写 `9/30 前` 这类绝对日期
- ❌ `尽快`、`越早越好` → 属于模糊词，见第 3 条

### 6. 循环任务

自动工具无法设置重复规则（Things AppleScript 限制）。
`每周六晨跑` 这类条目会归入区域但不排期，由用户手动设重复。
投递时保持自然表述即可，无需特殊标记。

### 7. 带上区域关键词

区域按语义判断（区域体系见 Mac 端 `~/.things-organizer/areas.json`），
关键线索决定去向。以 Work/Family/Personal 为例：

- `给A公司做宣传页` ✅（带线索）　`做宣传页` ⚠️（可能判不出，留在 Inbox）
- 拿不准归属时照常投递 —— 留在 Inbox 不动比归错区好

## 投递格式

```
标题：<日期，可选> <事项> <时刻，可选> <关键线索>
notes：补充信息（人名、地点、电话、背景）—— 归档时原样保留，鼓励写丰富
```

例：

```
标题：9/25 14:52 接人：G1234 上海虹桥→深圳北（03车02A）
notes：带伞；出站后走北广场
```
